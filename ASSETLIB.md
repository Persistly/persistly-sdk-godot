# Godot Asset Library Submission

Use this metadata for the Godot Asset Library submission.

## Asset

- Name: `Persistly`
- Category: `Plugins`
- License: `Apache-2.0`
- Godot version: `4.x`
- Repository: `https://github.com/Persistly/persistly-sdk-godot`
- Issues: `https://github.com/Persistly/persistly-sdk-godot/issues`
- Download/release: attach the `dist/persistly-godot-sdk-1.1.0.zip` generated by `scripts/package_release.sh`.

## Description

Persistly cloud saves for Godot games.

The addon provides a game-friendly `PersistlyGameSaves` facade for named save slots such as `autosave`, local-first save/load behavior, account data, safe force sync, due-slot sync, account sessions, and explicit local-vs-cloud conflict states.

Use it for idle games, incremental games, RPG-lite games, browser/native Godot projects, and single-player progression games that need cloud saves without building a full backend.

## Install Notes

The package includes:

```text
addons/persistly/
contracts/
```

Both folders are required. The account-first SDK expects `persistly-contract-v0.4.0` once the platform contract export is available; this checkout still includes the older fallback bundle and the validator reports that dependency.

## Tags

```text
cloud save, save sync, autosave, offline save, game backend, persistence, idle games, RPG, addon
```

## Pre-submit Checklist

- [ ] `plugin.cfg` version is `1.1.0`.
- [ ] `addons/persistly/README.md` exists.
- [ ] `addons/persistly/LICENSE` exists.
- [ ] `scripts/package_release.sh 1.1.0` creates the release ZIP.
- [ ] Godot headless validation passes.
- [ ] Live smoke passes with a dev/test runtime key.
